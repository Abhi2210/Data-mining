1) Python 3.5 is for this program.
2) since nlt is used so kindly run program on 3.x version of python
3) Just set the number of train and test data in the program in the below given loop
  "   for text in os.listdir(filename)[:500]:" --- for the train
  and 
  " for text1 in os.listdir(filename1)[500:]:"  ---test data.

  ex. the above loop is for 500 training and test data.
  